# Server-side only. Deliberately NOT NEXT_PUBLIC_ — the browser never calls
# the API directly; every request goes through a Server Component or Server
# Action so tokens stay in httpOnly cookies.
API_BASE_URL=http://localhost:3001/api/v1

# Set to 1 in production so session cookies are marked Secure.
COOKIE_SECURE=0
